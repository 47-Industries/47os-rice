# Installed to /etc/systemd/system/lightdm.service.d/
# If lightdm ever fails (custom greeter crashes / won't start),
# systemd runs the revert unit instead of leaving a black screen.
[Unit]
OnFailure=47os-greeter-revert.service
